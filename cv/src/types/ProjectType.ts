
export type ProjectType = {
    title: string; // Заголовок проекта
    logoSrc: string; // URL или путь к логотипу
    linkHref: string; // Ссылка на проект
    linkText: string; // Текст ссылки
    screenshotSrc: string; // URL или путь к скриншоту
    position: string; // Основная позиция (например, frontend)
    subPosition: string; // Дополнительная позиция или роли
    projectStatus: string;
  };
  